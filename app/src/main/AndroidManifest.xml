<?xml version="1.0" encoding="utf-8"?>
    <manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" package="com.example.SPQRisiko">

<application
    android:allowBackup="true"
    android:icon="@mipmap/eagle"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/eagle"
    android:supportsRtl="true"
    android:theme="@style/Theme.AppCompat">
    <!--         android:icon="@mipmap/ic_launcher"-->


    <!-- Attività principale (Home Page) -->
    <activity android:name=".MainActivity" android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>


    <!-- Pagina1 Activity -->
    <activity android:name=".FirstPageActivity" />
    <activity android:name=".GenerationOfRandomProvincesActivity" />


    <!-- Pagina2 Activity -->
    <activity android:name=".SecondPageActivity" />


</application>
</manifest>